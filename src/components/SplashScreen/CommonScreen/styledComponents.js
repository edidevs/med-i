import styled from 'styled-components/native';

import { GreetingText } from '../../../common/styledComponents';

export const SplashText = styled(GreetingText)`
  font-size: 24px;
  text-align: center;
  padding-top: 0px;
`;
